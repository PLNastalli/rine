[package]
name = "nt-sync"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "NT synchronization: events, mutexes, semaphores over futex"

[dependencies]
winabi.workspace = true
