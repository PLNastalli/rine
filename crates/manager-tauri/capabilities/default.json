{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Mínimo: só os commands do Manager + dialog para escolher arquivos.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
