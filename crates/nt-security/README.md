# nt-security

Tokens/SIDs/access checks. v0.1: stub tipado que permite (single-user local).

- Não é: enforcement (v0.5).
- API: `Token::current`, `check_access→SUCCESS`. Deps: `winabi`.
- Testes: —. Futuro: SIDs reais, `AccessCheck`, integridade.
