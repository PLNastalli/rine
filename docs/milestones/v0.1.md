# v0.1 — Hello World direto na CPU (CONCLUÍDO 2026-09-10)

Escopo: PE32+ x86_64 próprio; entry original na CPU; ntdll/kernel32 mínimas;
`hello.exe → "Hello World"`, exit 0; zero Wine/Proton/VM.

Critérios (todos ✅):
- [x] `cargo fmt --check`, `clippy -D warnings`, `test --workspace` verdes
- [x] `rine /tmp/hello.exe` imprime `Hello World` e sai 0
- [x] Teste E2E isolado (`launcher/tests/hello.rs`)
- [x] Docs base: architecture, current-state, roadmap, ADRs 0001–0006,
      safety-model, testing-strategy, subsystem docs v0.1
- [x] Sem `TODO` crítico escondido (dívidas em `current-state.md`)

Não-escopo (empurrado): tudo de v0.2+ (ver `roadmap.md`).
Riscos realizados: `written` em `.rdata` → SIGSEGV (corrigido; lição registrada
em `subsystems/pe-loader.md`).
