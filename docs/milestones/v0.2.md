# v0.2 — Processo e filesystem (EM ANDAMENTO, E2E verdes)

Escopo: `NtCreateFile/Read/Write`; drives da Capsule;
`VirtualAlloc/Free/Protect` públicos; registry por-capsule; `capsule.toml`;
`file.exe`, `args.exe`, `alloc.exe` + diferencial vs Windows.

Critérios de conclusão:
- [x] 3 PEs de teste passando no `rine` (`crates/launcher/tests/v02.rs`)
- [x] `capsule.toml` funcional + `compat/profiles/` documentado
- [x] Registry em arquivo (`registry.toml`, formato congelado)
- [ ] E2E diferenciais vs Windows real (mesmos PEs, oracles)
- [ ] Fechar milestone (esta doc + `current-state.md`)

Explicitamente NÃO (adiados p/ v0.3, ver current-state): `VirtualQuery`,
`openat2` direto, `X:rel`/UNC, share/flags, advapi32 guest, threads extras,
SEH, DLL loading.
