# 2026-09-10 — Fundação v0.1 (sessão inicial)

- O que mudou: workspace 19 crates; PE parser+builder; loader+relocs+imports;
  handles geracionais; nt-memory; façades `extern "win64"`; `Emulator` com
  GS/TEB; `rine` E2E verde (`Hello World`, exit 0); docs base + 6 ADRs.
- Por quê: base pequena, correta e documentada antes de qualquer feature.
- Arquivos importantes: `crates/pe/src/builder.rs`, `crates/runtime/src/lib.rs`,
  `crates/kernel32/src/lib.rs`, `crates/launcher/tests/hello.rs`.
- Testes: `cargo test --workspace` (todos ok); gdb usado para diagnosticar
  SIGSEGV (`written` em `.rdata` → movido para stack).
- Problemas: `libc` sem `ARCH_SET_GS`/`getpagesize` (constantes locais +
  `sysconf`); `HandleTable` com índice de reuso errado (teste pegou).
- Próximo passo: fechar leva de docs (READMEs por crate, development-log),
  gate final `fmt/clippy/test`, abrir v0.2.
