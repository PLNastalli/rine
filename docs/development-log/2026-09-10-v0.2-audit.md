# 2026-09-10 — Auditoria + v0.2 E2E (segunda sessão)

- Auditoria (política `docs/cleanup-policy.md`, registrada no repo): grep de
  TODOs (zero), código comentado (zero), nomes legacy (zero), consumidores
  por símbolo, deps Cargo por crate. Mortes comprovadas removidas (lista em
  `current-state.md`); ~40 deps especulativas podadas; `Thread`/`Process`
  cobertos por testes novos em vez de deletados.
- Reparada migração interrompida da sessão anterior (load 3 args, contexto
  com `mem`/`fsys`/`cmdline`, callers migrados, `run_hello` deletado).
- v0.2: `DriveMap`, `CreateFileA/ReadFile/CloseHandle`,
  `VirtualAlloc/Free/Protect`, `GetCommandLineW`, `capsule.toml`,
  `registry.toml`, builders `file/alloc/args.exe` + `tests/v02.rs` (3 E2E).
- Bugs: `sub rsp,imm8` com sinal; R9 não zerado (7-arg call); teste com
  `success()` quando exit code é dado. Todos com regressão (helpers +
  testes). Detalhes em `current-state.md`.
- Gate: fmt ✅, clippy `-D warnings` ✅, 34 passed/0 failed, hello
  bit-idêntico (`c9ba94…`).
- Próximo: fechar v0.2 (diferencial Windows) ou abrir v0.3.
