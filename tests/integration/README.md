# tests/integration — integração (harness atual)

O harness E2E vive em `crates/launcher/tests/hello.rs` (padrão Cargo para
`CARGO_BIN_EXE_rine`). Este dir guardará scripts multi-PE em v0.2.
Ver `docs/testing-strategy.md`.
