# tests/behavior — PEs por comportamento (futuro)

Um PE mínimo por comportamento (`file.exe`, `alloc.exe`, `args.exe`…),
gerados via `pe::builder` ou `tests/windows/`. Cada um com oracle esperado
documentado. v0.1: o único é o hello embutido em `crates/launcher/tests/hello.rs`.
